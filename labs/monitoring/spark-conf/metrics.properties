# Spark metrics configuration for Prometheus monitoring
# Enables PrometheusServlet sink (built-in since Spark 3.0)
# Endpoint: /metrics/prometheus/ on each component's web UI port

# PrometheusServlet sink for all components
*.sink.prometheusServlet.class=org.apache.spark.metrics.sink.PrometheusServlet
*.sink.prometheusServlet.path=/metrics/prometheus

# Enable JVM metrics for all components
master.source.jvm.class=org.apache.spark.metrics.source.JvmSource
worker.source.jvm.class=org.apache.spark.metrics.source.JvmSource
driver.source.jvm.class=org.apache.spark.metrics.source.JvmSource
executor.source.jvm.class=org.apache.spark.metrics.source.JvmSource
